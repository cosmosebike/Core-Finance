# 公路车库存表 — Memory 文档

> 本文件是 AI 生成公路车库存报表的完整参考。涵盖数据源规范、字段定义、业务规则、报表结构和构建方式。

---

## 一、数据源

### 文件来源
- **主库**：`宇宙销售统计中心.xlsx`（飞书表格，位于 Downloads/）
- **快照**：从主库手动导出「2026 商品SKU」工作表，存入 `数据快照/` 目录

### 命名规则
```
宇宙销售统计中心_2026 商品SKU_MMDDHHH.xlsx
```
- `MMDDHHH`：月日时，如 `031316` = 3月13日 16:00
- 报表中只显示日期（M/DD），不显示具体时间
- 当前最新版：`031316`

### 历史快照
| 文件 | 时间 | 备注 |
|------|------|------|
| `031114` | 3/11 14:00 | 初始版本 |
| `031214` | 3/12 14:00 | 增加库存级别分类 |
| `031313` | 3/13 13:00 | 级别调整 |
| `031314` | 3/13 14:00 | Pinarello/Dogma 分级完成 |
| `031316` | 3/13 16:00 | **当前版本**，15 个 SKU 成本价补录 |

---

## 二、字段定义（20 列）

| 列名 | 类型 | 用途 | 注意 |
|------|------|------|------|
| `名称` | 文本 | SKU 主键 | 有尾部空格，读取时必须 `.strip()` |
| `2026 滨江仓库数量` | 数字/字符串 | 滨江在库件数 | 部分版本为字符串，需 `float()` 转换 |
| `2026 常州数量` | 数字/字符串 | 常州在库件数 | 同上 |
| `品类` | 枚举 | 商品大类 | 见品类列表 |
| `系列` | 文本 | 品牌/产品线 | 可为空 |
| `组合明细` | 文本 | 规格（尺码/颜色） | 辅助字段 |
| `整车配置` | 文本 | 整车套件配置 | 仅整车类有值 |
| `库存级别` | 枚举 | **核心分类**，决定报表分组和优先级 | 见级别定义 |
| `2026 库存产品` | 文本 | 仓库+SKU 拼接，供关联查询 | |
| `处理方式` | 文本 | 清理策略 | 清理类才有值 |
| `瑕疵标记` | 文本 | 商品瑕疵描述 | 可为空 |
| `实际成本价` | 数字 | **单件成本** | ⚠️ 部分历史版本为 0，见第四节 |
| `售价` | 数字 | 建议零售价 | 可为空 |
| `2026 总库存成本` | 文本/数字 | 公式结果（件数×成本价） | `data_only` 读取时可能为字符串 |
| `库存产品验证` | 文本 | ✅ 表示已验证 | 质量标记 |
| `其他销售备注` | 文本 | 补充说明 | Sheet4 备注列来源 |
| `清理底价` | 数字 | 最低可接受清理售价 | 仅 Pinarello 清理 / 清理库存类填写 |
| `仓储位置` | 文本 | 主仓（滨江仓库/常州） | |
| `2026 入库单` | 文本 | 关联 2026 入库记录 | |
| `2025 入库单` | 文本 | 关联 2025 入库记录 | |

---

## 三、库存级别定义

报表所有 Sheet 的分组、排序、高亮均以此为基准。

| 优先级 | 级别 | 图标 | 数量(031316) | 业务含义 |
|--------|------|------|-------------|---------|
| 1 | `Dogma 系列` | 🔵 | 19 | 主销公路车，正常出货，重点跟踪 |
| 2 | `Pinarello 清理` | 🔴 | 21 | 有时限清理目标，需跟踪售价和进度 |
| 3 | `清理库存` | 🔴 | 52 | 非 Pinarello 清理品，按负责人跟踪 |
| 4 | `正常商品` | 🟢 | 78 | 主流在售，关注流通速度和现金流 |
| 5 | `DA/Zipp 轮组` | 🔵 | 5 | 高价值轮组，独立跟踪 |
| 6 | `换货回商品` | 🟡 | 5 | 售后换回，成本待核定 |
| 7 | `自营产品` | 🟢 | 6 | Oi! 等自有品牌，生产制，有 BOM 成本 |
| 8 | `套件类` | 🟢 | 32 | DA/UT/RED 套件及散件 |
| 9 | `FSA 系列` | 🟡 | 4 | FSA 品牌商品 |
| 10 | `配件` | ⚪ | 125 | 通用配件，SKU 数最多 |
| 11 | `铺货商品` | ⚪ | 22 | 代销/铺货品 |

**高流通级别**（Sheet3 库存变动对比仅含这些）：
`正常商品`、`DA/Zipp 轮组`、`换货回商品`、`自营产品`、`套件类`、`FSA 系列`、`配件`、`铺货商品`

**清理级别**（Sheet4 清理库存跟踪仅含这些）：
`Pinarello 清理`、`清理库存`

---

## 四、品类优先级（报表内排序用）

```
整车(1) > 车架(2) > 轮组(3) > 外胎(4) > 盘片/碟片组(5) >
曲柄组/牙盘组(6) > 功率计(7) > 座垫(8) > 车把一体把(9) >
套件组/套件散装/套件链条(10) > 后拨导轮(11) > 内胎(12) >
把带(13) > 中轴(14) > 锁踏/脚踏(15) > 骑行台(16)
```

同级别内排序：`库存级别 → 品类优先级 → 系列（字母序）→ SKU名称（字母序）`

---

## 五、成本价注意事项

### 031316 已补录的 15 个 SKU

以下 SKU 在 `031214` 中成本价为 0，`031316` 已更新。  
**跨版本比对时，旧版本成本价统一回退到当前值（避免虚假成本变动）：**

| SKU 名称 | 成本价 | 级别 |
|---------|--------|------|
| PCW 轮组 古铜金6560 | ¥25,000 | 清理库存 |
| RIDLEY 整车 紫色 M码（带车把） | ¥16,500 | 清理库存 |
| GUSTO 整车 GTR 白色 M | ¥11,800 | 清理库存 |
| Cline 整车 火焰清漆M2 | ¥10,888 | 清理库存 |
| 闪电 车把一体把 roval一体把420-100 | ¥3,100 | 正常商品 |
| ILLUSION 轮组 【暗标】钢条 40 | ¥2,300 | 自营产品 |
| RIVAL 套件组 基础配置无前拨（13速） | ¥4,400 | 正常商品 |
| RIVAL 碟片组 2张160 | ¥140 | 正常商品 |
| RIVAL 链条 E1 链条 | ¥185 | 正常商品 |
| DA 牙盘组 52-36T 170 | ¥1,200 | 正常商品 |
| SRAM 其他小配件 电池 | ¥220 | 配件 |
| SRAM 其他小配件 充电器 | ¥250 | 配件 |
| SENICX 驭勇 中轴 钢球轴承 T47-85.5-29 | ¥118 | 正常商品 |
| Selle Italia 座垫 新款钢轨黑色（散装） | ¥500 | 正常商品 |
| Selle Italia 座垫 新款碳轨黑色（散装） | ¥1,100 | 正常商品 |

### 仍有库存但成本价为 0（待人工补录）

WAHOO 骑行台系列（4款）、帕索尼 轮组 五福 / 车架 虎纹 54、
Dogma F【25款】Myway 系列车架（5款）、BMC 车架 蓝红色 47、
LOOK 车架（2款）、SPECIALIZED 车架 SL7 彩虹冰裂纹

---

## 六、清理库存负责人分配

Sheet4（清理库存跟踪）按负责人分组，当前分配如下：

| 负责人 | 说明 |
|--------|------|
| 张振 | 分两个子组：① Pinarello/Dogma 清理 ② 非 Pinarello |
| 郭城 | |
| 潘昊 | |
| 海尔 | |
| 程总 | |
| (待确认) | 未匹配到负责人的 SKU 归此组 |

**DEDA 车把一体把分配规则（按件数比例）：**
- 郭城：40-100 / 40-110 / 44-100 / 44-120（共 ~43 件，≈83%）
- 张振：44-110 / 42-100 / 42-110（共 ~9 件，≈17%）

**负责人来源**：从 `公路车库存_20260312_v6.xlsx` 的「库存清理跟踪」Sheet 自动读取，加手动覆盖映射表（见构建脚本 `overrides` 字典）。

---

## 七、报表结构（公路车库存_YYYYMMDD_vX.xlsx）

### Sheet 1：总览
- 各库存级别的 SKU 数量、总件数、总成本、占比
- 全部为 Excel 公式驱动（`=SUM()`，`=E/总计E`）

### Sheet 2：库存 SKU 明细
- 全量 SKU，按「库存级别 → 品类 → 系列 → SKU名称」排序
- 每级别前有分组标题行，后有小计行
- 总件数（`=滨江+常州`）和总成本（`=总件数×成本价`）均为公式
- 成本价、总成本居中；SKU名称左对齐

### Sheet 3：库存变动对比
- 仅含高流通级别，仅显示件数有变动的 SKU
- 顶部 1 行文字摘要 + 1 行 KPI 汇总（上次总成本 / 本次总成本 / 成本变动 / 变动件数）
- 对比列：上次件数/成本（PREV快照）vs 本次件数/成本（CURR快照）
- 负变动（出库）用勃艮第红 `#8B1A1A` 标注

### Sheet 4：清理库存跟踪
- 仅含 `Pinarello 清理` + `清理库存` 两个级别
- 按负责人分组，张振再分 Pinarello/非 Pinarello 子组
- 列结构：SKU名称 / 品类 / 系列 / 期初件数(2/8) / 期初总成本(2/8) / 3/12件数 / 3/12成本 / 3/13件数 / 3/13成本 / 已清件数 / 已清成本 / 4/15目标 / 4/15实际 / 完成率 / 6/15目标 / 备注
- 已清件数 = `=期初-当前`，完成率 = `=IFERROR(实际/目标,"—")`
- 4/15目标、4/15实际、6/15目标列用琥珀色 `#FFF8E8` 标注（待手动填写）
- 每组后有小计行，最底部有合计行

---

## 八、构建脚本

| 项目 | 说明 |
|------|------|
| 脚本文件 | `build_0313_v3.py`（根目录） |
| 当前数据源（CURR） | `数据快照/宇宙销售统计中心_2026 商品SKU_031316.xlsx` |
| 对比基准（PREV） | `数据快照/宇宙销售统计中心_2026 商品SKU_031214.xlsx` |
| 期初数据 | `~/Downloads/2026清货KPI管理表_表1表2.xlsx`（Sheet：表1-库存分类总览） |
| 负责人来源 | `库存报表/公路车库存_20260312_v6.xlsx`（Sheet：库存清理跟踪） |
| 输出文件 | `库存报表/公路车库存_YYYYMMDD_vX.xlsx` |

### 更新数据源的步骤

1. 将新 SKU 快照保存到 `数据快照/`，命名格式：`宇宙销售统计中心_2026 商品SKU_MMDDHHH.xlsx`
2. 修改脚本中 `FILE_CURR` 指向新文件
3. 若需更换对比基准，同步修改 `FILE_PREV`
4. 修改 `OUT` 文件名（递增版本号）
5. 执行 `python3 build_0313_v3.py`

### 关键辅助函数

```python
def to_num(v):
    """安全转数值，处理字符串/None/nan/inf"""
    try:
        r = float(v) if v is not None else 0.0
        return 0.0 if math.isnan(r) or math.isinf(r) else r
    except:
        return 0.0

def normalize_sku(s):
    """标准化 SKU 名称，用于跨快照版本匹配"""
    return ' '.join(str(s).strip().split()).lower()

def get_qty(row_dict):
    """读取总在库件数"""
    return to_num(row_dict.get('2026 滨江仓库数量')) + \
           to_num(row_dict.get('2026 常州数量'))
```

---

## 九、数据审核机制

### 9.1 每次更新报表的操作流程

```
1. 从飞书导出最新「2026 商品SKU」工作表
2. 命名为 宇宙销售统计中心_2026 商品SKU_MMDDHHH.xlsx
3. 放入 数据快照/ 目录
4. 修改 build_0313_v3.py 中两行变量：
     FILE_CURR = f"{SNAP}/宇宙销售统计中心_2026 商品SKU_新文件名.xlsx"
     FILE_PREV = f"{SNAP}/宇宙销售统计中心_2026 商品SKU_上一个文件名.xlsx"
5. 更新 OUT 版本号（v13 → v14 …）
6. 运行脚本：python3 build_0313_v3.py
7. 阅读终端输出的「数据质量审核报告」
8. 按报告指示处理问题后再使用报表
```

---

### 9.2 自动审核报告（脚本内置）

每次生成报表后，`run_audit()` 函数自动扫描并输出三类警告：

| 级别 | 含义 | 处理方式 |
|------|------|----------|
| 🔴 必须修复 | 有库存但成本价=0，金额计算不可信 | 在飞书表格补录成本价 → 重新导出快照 → 重新生成 |
| 🟡 建议确认 | 期初成本=0，已自动用当前价回填 | 核实回填值是否正确（尤其是清货类SKU） |
| 🔵 信息提示 | 3/12成本价=0已回退，或新增清理SKU | 知悉即可，不影响准确性 |

**规则**：若🔴为0，报告末尾显示 `✅ 数据质量良好`，否则显示 `⚠️ 建议补录后重新生成`。

---

### 9.3 自动回填规则（脚本逻辑）

以下情况脚本会自动补数，但每次都会在审核报告中提示：

| 场景 | 自动处理 | 审核级别 |
|------|----------|----------|
| 有库存，当前成本价=0 | **不回填**，直接报错 | 🔴 必须修复 |
| 3/12成本价=0 | 用当前成本价替代 | 🔵 信息提示 |
| 期初有件数，期初成本=0 | 用当前成本价 × 期初件数 | 🟡 建议确认 |
| 期初无记录，当前有库存 | 期初 = 当前（视为未发生清货） | 🟡 建议确认 |
| 期初无记录，当前=0（已售清） | 如期初KPI有记录则用KPI；否则不出现在Sheet4 | - |

---

### 9.4 每次更新的人工检查清单

> 拿到新数据快照后，在生成报表前确认以下事项：

- [ ] 新快照文件是否已放入 `数据快照/` 目录？
- [ ] `FILE_CURR` 是否指向最新文件，`FILE_PREV` 是否为前一版本？
- [ ] 🔴 审核报告中成本价=0的SKU是否已在飞书补录？（未补录则不生成）
- [ ] 🟡 期初成本回填的SKU，金额是否和实际采购价一致？
- [ ] Sheet 4 中各人员小计行的目标金额（N/P列）是否需要从上一版本复制？（每次重新生成后目标列会清空）
- [ ] 是否有新增清理SKU（🔵报告中"新增清理SKU"提示）？如有，需在脚本中分配负责人

---

### 9.5 版本归档规则

| 目录 | 存放内容 | 命名规范 |
|------|----------|----------|
| `数据快照/` | 飞书导出的原始快照 | `宇宙销售统计中心_2026 商品SKU_MMDDHHH.xlsx` |
| `库存报表/` | 脚本生成的报表 | `公路车库存_YYYYMMDD_vN.xlsx` |

**不删除旧版本**：快照和报表都保留历史，文件名本身即版本标识。

---

### 9.6 已知成本价异常 SKU（待补录）

以下 SKU **有库存但成本价尚未录入**，若出现在🔴报告中，优先处理：

| SKU 名称 | 备注 |
|----------|------|
| WAHOO 骑行台系列（4款） | 铺货商品，可暂缓 |
| 帕索尼 轮组 五福 / 车架 虎纹 54 | 铺货商品，可暂缓 |
| Dogma F【25款】Myway 系列车架（5款） | 铺货商品，可暂缓 |
| BMC 车架 蓝红色 47 | 铺货商品，可暂缓 |
| LOOK 车架（2款） | 铺货商品，可暂缓 |
| SPECIALIZED 车架 SL7 彩虹冰裂纹 | 铺货商品，可暂缓 |

> 铺货商品成本价为0不影响库存金额统计（已从汇总中排除），但正常商品/清理库存成本价为0**必须补录**。

---

## 十、STYLE1 设计规范（摘要）

完整规范见 `STYLE1_DATA_SPEC_副本.md`。

| 色彩 | HEX | 用途 |
|------|-----|------|
| 近黑 | `#111111` | 标题背景、表头背景 |
| 纸白 | `#FAF9F6` | 页面底色、副标题背景 |
| 行条纹 | `#F5F5F5` | 奇偶行交替 |
| 小计背景 | `#E8E8E8` | 小计/合计行 |
| 勃艮第红 | `#8B1A1A` | 负变动、清理类标题 |
| 琥珀提示 | `#FFF8E8` | 待填写目标列 |

| 字体 | 用途 |
|------|------|
| Calibri 10pt | 所有数据单元格 |
| Calibri 14pt Bold 白色 | 报表主标题 |
| Calibri 10pt Bold 白色 | 列标题 |
| Calibri 9pt 灰色斜体 | 底部注释行 |

**数字格式：**
- 件数：`#,##0`
- 成本/金额：`#,##0`（不显示小数）
- 占比/完成率：`0.0%`

**对齐规则：**
- SKU名称、处理方式/备注：左对齐
- 其余所有列（品类、系列、数字）：居中

**结构规则：**
- 第 A 列留白（宽度 2），内容从 B 列开始
- 每 Sheet 顶部：主标题行（高 32）+ 副标题行（高 18）+ 空白行（高 6）
- 所有合计/小计必须使用 Excel 公式（`=SUM()`），不允许写死数值

---

## 十一、Excel 生成铁律（所有脚本必须遵守）

> 本节规则适用于所有 Python 生成的 Excel 文件，违反任何一条视为构建错误，必须修复后才能交付。

### 11.1 小计与合计必须是公式

**每一个小计行、合计行的数值单元格，必须写入 Excel SUM 公式，绝对不允许：**
- 写死 Python 计算出来的数字（如 `value=total_qty`）
- 留空（`value=None` 或 `value=""`）
- 写文字（如 `value="—"`）

**正确写法：**
```python
# ✅ 正确：公式
ws.cell(row=subtotal_row, column=col, value=f"=SUM(D{data_start}:D{data_end})")

# ❌ 错误：写死数值
ws.cell(row=subtotal_row, column=col, value=total_qty)

# ❌ 错误：留空
ws.cell(row=subtotal_row, column=col, value=None)
```

### 11.2 条件公式

当值为 0 时显示空白，不显示 0，使用：
```python
f"=IF(SUM(...)=0,\"\",SUM(...))"
```

### 11.3 百分比公式

完成率等除法公式必须防除零错误：
```python
f"=IF(OR(N{row}=\"\",L{row}=\"\"),\"\",L{row}/N{row})"
```

### 11.4 检查清单（每次生成后必查）

在任何 Excel 文件交付前，确认以下所有项均为公式：
- [ ] 每个分组的小计行（件数列、金额列）
- [ ] 每个 Sheet 的总计行（件数列、金额列）
- [ ] 完成率、占比等派生计算列
- [ ] Sheet 1 总览中引用其他 Sheet 的汇总数字

---

*最后更新：2026-03-13 · 对应报表版本：公路车库存_20260313_v14.xlsx*
