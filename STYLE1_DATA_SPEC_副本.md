# STYLE 1 · 权威咨询风 — AI 执行规范
## 数据类物料完整设计语言系统

> **适用范围：** 数据中台项目、分析报告、对外咨询文件、Excel 数据表、Word/PDF 报告
> **核心原则：** 一切都是信息。设计的存在是为了让数据更可读，而不是为了让人注意到设计本身。
> **参照标杆：** McKinsey 内部报告风格 · 彭博终端数据密度 · BCG 对外白皮书

---

## PART 1 · 设计哲学（AI 必读，决定所有执行判断）

### 1.1 三条铁律

**铁律一：克制即专业。**
禁止为了"好看"添加任何元素。每一个颜色、每一条线、每一个间距，都必须有信息传递的理由。

**铁律二：层级即导航。**
读者在 3 秒内必须能判断：这份文件讲什么、最重要的数字在哪里、下一步看哪里。视觉层级是读者的地图。

**铁律三：数字不说谎。**
所有数字对齐方式统一为右对齐，小数位数在同一列内统一，单位标注永远在列标题里，不在单元格里。

### 1.2 风格气质关键词

`权威` · `克制` · `信息密集` · `可信` · `无冗余` · `专业工具感`

### 1.3 禁止清单（AI 绝对不能做的事）

| 禁止项 | 原因 |
|--------|------|
| 渐变背景 | 分散注意力，降低可信度 |
| 装饰性图标 | 无信息价值，幼稚化 |
| 超过 3 种颜色的图表 | 信息过载，颜色失去区分功能 |
| 圆角超过 4px 的表格 | 削弱严肃感 |
| 在数据单元格内使用强调色背景 | 破坏扫视节奏 |
| 标题使用全大写且字号超过 28pt | 视觉暴力 |
| 同一页面超过 2 种字体 | 混乱，不专业 |
| 数字列左对齐 | 无法快速比较大小 |
| 在图表里加 3D 效果 | 失真数据，不可信 |
| 使用系统默认 Excel 蓝色表格样式 | 低级感 |

---

## PART 2 · 色彩系统

### 2.1 主色板

```
PRIMARY PALETTE — Style 1 · 权威咨询风

┌────────────────────────────────────────────────────────────┐
│  PAPER WHITE     #FAF9F6   所有文件底色，比纯白更温和      │
│  PURE WHITE      #FFFFFF   表格单元格、卡片内部            │
│  NEAR BLACK      #111111   所有正文、数字、主要文字        │
│  MUTED GREY      #555555   次要信息、日期、来源标注        │
│  LIGHT GREY      #CCCCCC   分隔线、表格细边框              │
│  ROW STRIPE      #F5F5F5   斑马纹浅行背景（每隔一行）      │
│  HEADER BG       #111111   表头背景色                      │
│  HEADER TEXT     #FFFFFF   表头文字（白色反白）             │
│  BURGUNDY RED    #8B1A1A   唯一强调色（见使用条件↓）       │
│  SUBTLE BORDER   #E8E8E8   卡片/区块的外边框               │
└────────────────────────────────────────────────────────────┘
```

### 2.2 勃艮第红 `#8B1A1A` 使用条件（严格执行）

**允许使用的情况（仅限以下 5 种）：**
1. H2 节标题下方的 2px 实线装饰线
2. 报告封面上的主数字（唯一一个最重要的结论数字）
3. 数据表中"负增长/警示"类数值（替代红色警告）
4. 关键词第一次出现时的下划线（每份文件不超过 3 处）
5. 页眉/页脚中的品牌标识线

**绝对禁止的情况：**
- 大面积背景色
- 图表填充色（图表有自己的色板，见 Part 5）
- 普通正文的加粗或高亮
- 纯装饰性使用

### 2.3 图表专用色板（6 色，有序使用）

```
图表色序（必须按此顺序，不能跳色）：

1st  #2C3E6B   深海蓝（主系列，最重要数据）
2nd  #8B1A1A   勃艮第红（对比系列或第二重要）
3rd  #5C5C5C   中性灰（背景/参考系列）
4th  #A8B4C8   浅钢蓝（次要系列）
5th  #C4A882   沙金色（特殊分类）
6th  #3D6B4F   松绿（正向/增长标注）
```

**图表用色铁律：**
- 单一数据系列：只用 `#2C3E6B`
- 两个系列对比：`#2C3E6B` vs `#8B1A1A`
- 三个及以上系列：按色序排列，不得使用品牌强调色之外的鲜艳色
- 目标线/基准线：永远用 `#111111` 虚线

---

## PART 3 · 字体系统

### 3.1 字体栈定义

```
FONT STACK — Style 1

─── 标题字体（Serif 衬线体）─────────────────────────────
  中文首选：Noto Serif SC · 方正书宋 · 宋体
  英文首选：Georgia · Times New Roman
  用途：H1、H2、报告封面大标题、章节名称
  气质：权威、历史感、严肃的可信度

─── 正文字体（Sans 无衬线体）────────────────────────────
  中文首选：PingFang SC · 微软雅黑（Microsoft YaHei）
  英文首选：Inter · Helvetica Neue · Arial
  用途：H3、正文段落、说明文字、表格内文字
  气质：清晰、现代、高可读性

─── 数字/数据字体（等宽感无衬线）────────────────────────
  首选：Calibri · Source Sans Pro · Tabular figures
  英文备选：Roboto · DIN · Arial
  用途：所有数字、百分比、表格数据列、坐标轴标注
  气质：精确、对齐感强、Bloomberg 数字感
  注意：必须确保数字等宽（tabular figures），保证列对齐

─── 标注字体（小号无衬线）──────────────────────────────
  与正文字体相同，降低字号
  用途：数据来源注释、脚注、图表标注、版权信息
```

### 3.2 字号层级系统（报告场景）

```
TYPOGRAPHY SCALE — 报告/PPT/Word

Level    用途              中文字号    英文字号    字重      颜色
──────────────────────────────────────────────────────────────
H0       封面主标题        36pt        32pt       Bold      #111111
H1       章节标题          24pt        22pt       Bold      #111111
         （配勃艮第红下划线，2pt，宽度与文字等宽）
H2       小节标题          18pt        16pt       SemiBold  #111111
H3       段落标题          14pt        13pt       Bold      #111111
         （全大写 + 字间距 +1）
Body     正文              12pt        11pt       Regular   #111111
Body-S   次要说明          11pt        10pt       Regular   #555555
Caption  图表说明/来源     10pt        9pt        Regular   #555555
Footnote 脚注/版权         9pt         8pt        Regular   #888888
Data     表格内数字        11pt        10pt       Regular   #111111
         （数字列右对齐，文字列左对齐）
KPI      封面关键数字      48-72pt     同         Bold      #8B1A1A 或 #111111
```

### 3.3 字号层级系统（Excel 场景）

```
TYPOGRAPHY SCALE — Excel

Level    用途              字号    字重      颜色
──────────────────────────────────────────
Sheet-H  工作表标题行       14pt   Bold      #FFFFFF（白字，底色 #111111）
Col-H    列标题             11pt   Bold      #FFFFFF（白字，底色 #111111）
Sub-H    分组标题行         11pt   Bold      #111111（底色 #F5F5F5）
Data     数据行             10pt   Regular   #111111
Total    合计/小计行        10pt   Bold      #111111（底色 #E8E8E8 + 上边框 2pt）
Note     备注行/来源        9pt    Regular   #888888
```

---

## PART 4 · Excel 执行规范

### 4.1 Excel 表格结构规则

```
EXCEL STRUCTURE — 标准布局

行号    内容              格式
─────────────────────────────────────────────────────────
Row 1   表格标题          合并单元格，14pt Bold，#111111，
                          底色 #111111（白字），高度 32px
Row 2   副标题/时间范围   合并单元格，10pt Regular，#555555，
                          底色 #FAF9F6，高度 24px
Row 3   空行              高度 8px，纯白色（视觉呼吸）
Row 4   列标题            11pt Bold，白字，底色 #111111，高度 28px
Row 5+  数据行            10pt Regular，斑马纹（#FFFFFF/#F5F5F5），高度 22px
末行    合计/总计行        10pt Bold，底色 #E8E8E8，
                          上边框 2pt #111111
末+1    数据来源注释       9pt Regular，#888888，左对齐，
                          前缀必须写"数据来源："
```

### 4.2 列宽标准规则

| 列类型 | 最小宽度 | 推荐宽度 | 内容对齐 |
|--------|----------|----------|----------|
| 序号列 | 30px | 40px | 居中 |
| 文字分类列（短） | 80px | 100px | 左对齐 |
| 文字分类列（长名称） | 120px | 160px | 左对齐 |
| 数字数据列 | 70px | 90px | **右对齐（铁律）** |
| 百分比列 | 60px | 80px | **右对齐（铁律）** |
| 日期列 | 80px | 100px | 居中 |
| 备注/描述列 | 150px | 200px+ | 左对齐 |

### 4.3 数字格式规则

```
NUMBER FORMATTING RULES — 严格执行

类型          格式规则                      示例
────────────────────────────────────────────────────
整数           千位分隔符，无小数           1,234,567
金额（元）     千位分隔符，2位小数          ¥ 1,234,567.89
金额（万元）   千位分隔符，1位小数 + 单位在列标题  1,234.5
百分比         1位小数 + % 符号             23.5%
倍数           2位小数 + x                  3.2x
增速           带正负号，1位小数 + %        +12.3% / -5.8%
大数字缩写     亿级：X.X亿；万级：X,XXX万  2.3亿 / 8,400万

禁止格式：
× 百分比写成小数（0.235 ← 错误，应为 23.5%）
× 金额列混用带单位和不带单位
× 同列小数位数不统一
× 负数用括号表示（业务数据一律用减号）
```

### 4.4 边框系统

```
BORDER RULES — Excel

位置                规格
────────────────────────────────────
外边框              2pt 实线，#111111
列标题行下边框      2pt 实线，#111111
合计行上边框        2pt 实线，#111111
数据行横向分隔      1pt 实线，#CCCCCC（仅横向）
列分隔              无边框（用列宽+对齐方式区分）
最末行下边框        1pt 实线，#CCCCCC

禁止：
× 每个单元格四周都画边框（密网格感，低端）
× 使用彩色边框
× 斜线表头
```

### 4.5 条件格式规则

```
CONDITIONAL FORMATTING — 允许使用的规则

规则类型           触发条件              样式
─────────────────────────────────────────────────────
数据条             数字列（单一系列对比） 深海蓝 #2C3E6B 填充，无边框
负值高亮           数字 < 0              字体颜色 #8B1A1A（勃艮第红）
超出阈值           > 定义的目标值        字体颜色 #3D6B4F（松绿）
空值标注           ISBLANK()            底色 #FFF8F8（极淡粉提醒）
前3名              RANK() ≤ 3           Bold + 无其他样式

禁止：
× 颜色比例尺（红绿渐变热图）← 在数据报告中显得不专业
× 图标集（箭头、红绿灯图标）← 过于花哨
× 超过 2 种条件格式同时作用于一列
```

### 4.6 图表（嵌入 Excel）规则

```
CHART RULES — Excel 内嵌图表

图表类型选择规则：
- 时间序列趋势      → 折线图（Line Chart）
- 分类对比          → 水平条形图（Horizontal Bar）← 默认首选
- 构成/占比         → 堆叠条形图（Stacked Bar），禁止饼图
- 两变量相关        → 散点图（Scatter）
- KPI 单数字展示   → 无图表，大号数字直接放单元格

通用图表样式规则：
- 背景色：透明或 #FFFFFF
- 网格线：1pt，#E0E0E0，仅水平方向
- 坐标轴：显示，颜色 #555555，无箭头
- 图例：图表右侧或底部，不遮挡数据
- 数据标签：仅在数据点 ≤ 8 个时显示；超过 8 个不显示（用坐标轴读）
- 标题：图表标题与工作表标题风格一致，12pt Bold #111111
- 图表尺寸：宽度不超过表格总宽度，高度比例 16:9 或 4:3
```

---

## PART 5 · 报告执行规范（Word / PPT / PDF）

### 5.1 页面布局系统

```
PAGE LAYOUT — A4 报告

┌──────────────────────────────────────────────────────────┐
│  页边距                                                   │
│  上：25mm  下：25mm  左：30mm  右：25mm                  │
│                                                           │
│  页眉：10mm，内容：左-品牌标识 / 右-报告名称             │
│  页脚：10mm，内容：左-保密级别 / 右-页码（共X页）        │
│  页眉/页脚与正文分隔：1pt实线 #111111（顶部）           │
│                                                           │
│  正文版心宽度：155mm（30mm+155mm+25mm = 210mm）          │
│  内容列数：1列（叙事）或 2列（1:2比例，数据+说明）       │
└──────────────────────────────────────────────────────────┘

PPT 16:9 布局（1920×1080px）：

安全边距：上下各 72px，左右各 96px
内容区域：宽 1728px，高 936px
标题区：上部高 160px，下边 2px勃艮第红线
内容区：剩余空间，自由布局
页码区：右下角 80×40px
```

### 5.2 PPT 页面类型系统

```
PPT SLIDE TYPES — 每种类型有固定布局规则

Type A · 封面页（Cover）
  - 背景：#111111 全深色
  - 主标题：H0，白色，左对齐，居中 1/3 处
  - 副标题：Body，#888888，主标题下 16px
  - 日期/项目编号：右下角，10pt，#555555
  - 装饰元素：仅允许一条 2px 勃艮第红水平线

Type B · 目录页（Agenda）
  - 背景：#FAF9F6
  - 章节列表：编号 + 章节名，H2 级，带悬挂缩进
  - 当前章节：勃艮第红 #8B1A1A 高亮
  - 右侧：可留空或放置极简图形

Type C · 章节封面（Section Break）
  - 背景：#FAF9F6
  - 章节编号：96pt，极浅灰 #EBEBEB，装饰性数字（背景层）
  - 章节标题：H1，#111111，左对齐
  - 下方：1px线 #CCCCCC

Type D · 内容页·纯文字（Text Content）
  - 标题区：H2 + 勃艮第红线
  - 内容区：最多 4 个 bullet point，每条 ≤ 25 字
  - 禁止文字超过整页 60%

Type E · 内容页·数据图表（Data Slide）
  - 标题区：H2 + 勃艮第红线
  - 图表区：占 70% 版面
  - 洞察标注：右侧或底部，Body-S，说明最重要结论
  - 数据来源：左下角，Caption，必填

Type F · 内容页·数据表格（Table Slide）
  - 标题区：H2 + 勃艮第红线
  - 表格：占 75% 版面，遵循 Part 4 Excel 表格规则
  - 每页最多 10 行数据（超过则分页）
  - 高亮行规则：仅允许灰色（#E8E8E8）高亮，不用彩色

Type G · KPI 展示页（Big Number）
  - 最多放 3 个 KPI
  - 每个 KPI：大号数字（48-72pt）+ 单位 + 3-5字说明
  - 数字颜色：#111111（正常）或 #8B1A1A（强调唯一重点）
  - 禁止在同一页面上 3 个数字都用强调色

Type H · 结论/建议页（Recommendation）
  - 顶部：一句话结论，H1，勃艮第红线下划线
  - 下方：3 条建议，编号式，H3 + Body
  - 禁止使用 bullet points，用 01 / 02 / 03 数字编号
```

### 5.3 文字密度规则

```
INFORMATION DENSITY RULES — 防止信息过载

PPT 单张幻灯片上限：
- 标题：1条（最多 20 字）
- 文字段落：不超过 4 条 bullet，每条不超过 25 字
- 数据点：不超过 8 个（图表中）
- 颜色：不超过 3 种
- 字体层级：不超过 2 种

如果内容超出上限，必须拆分成多张幻灯片，不得缩小字号填塞。

Word/PDF 报告单页密度：
- 文字行数上限：40 行（A4，12pt）
- 每段最大字数：200 字（超过必须分段）
- 表格单页最多行数：20 行（含标题）
```

### 5.4 数据来源标注规则（强制执行）

所有包含数据的页面/图表/表格，**必须**在左下角或图表底部注明：

```
格式模板：
数据来源：[来源名称]，[时间范围]
示例：数据来源：公司内部 CRM 系统，2024Q1-Q4
示例：数据来源：国家统计局，2023 年年度数据
```

---

## PART 6 · AI 执行决策树

当 AI Agent 收到任务时，按以下决策树判断执行规则：

```
收到任务
  │
  ├─ 输出格式？
  │    ├─ Excel → 执行 Part 4
  │    ├─ PPT → 执行 Part 5（PPT 部分）
  │    ├─ Word/PDF → 执行 Part 5（报告部分）
  │    └─ 其他 → 询问确认
  │
  ├─ 内容类型？
  │    ├─ 数字密集（>50% 内容是数字/表格）→ 优先 Type E/F/G
  │    ├─ 叙事为主（分析、建议、洞察）→ 优先 Type D/H
  │    └─ 混合 → Type D + Type E 交替
  │
  ├─ 受众是谁？
  │    ├─ 对外客户/合作方 → 确保 Type A 封面 + 数据来源标注
  │    ├─ 内部团队 → 可省略封面，保留数据来源
  │    └─ 高管汇报 → 必须有 Type G KPI页 + Type H 结论页
  │
  └─ 颜色选择？
       ├─ 图表系列 1 个 → 只用 #2C3E6B
       ├─ 图表系列 2 个 → #2C3E6B + #8B1A1A
       ├─ 图表系列 3+ 个 → 按色序 Part 2.3 执行
       └─ 强调单个数字 → #8B1A1A（全页只能 1 个）
```

---

## PART 7 · 快速参考卡（Quick Reference）

```
╔══════════════════════════════════════════════════════════════╗
║  STYLE 1 · 权威咨询风 · 速查卡                              ║
╠══════════════════════════════════════════════════════════════╣
║  底色      #FAF9F6  主文字  #111111  强调色  #8B1A1A        ║
║  次要文字  #555555  边框    #CCCCCC  表头底  #111111        ║
╠══════════════════════════════════════════════════════════════╣
║  标题字体  Noto Serif SC / Georgia                          ║
║  正文字体  PingFang SC / Inter / 微软雅黑                   ║
║  数字字体  Calibri / Source Sans Pro（等宽数字）            ║
╠══════════════════════════════════════════════════════════════╣
║  H1  24pt Bold Serif   H2  18pt SemiBold Serif             ║
║  H3  14pt Bold Sans    Body 12pt Regular Sans              ║
║  Data 10-11pt Calibri  Caption 10pt #555555                ║
╠══════════════════════════════════════════════════════════════╣
║  图表色序：#2C3E6B → #8B1A1A → #5C5C5C → #A8B4C8         ║
╠══════════════════════════════════════════════════════════════╣
║  数字：右对齐  文字：左对齐  标题：左对齐或居中            ║
║  小数位数：同列统一  百分比：1位小数  金额：2位小数        ║
╠══════════════════════════════════════════════════════════════╣
║  PPT 单页上限：4条文字 / 8个数据点 / 3种颜色               ║
║  Excel 表头：白字深底  数据行：斑马纹  合计：粗上边框      ║
╚══════════════════════════════════════════════════════════════╝
```

---

*文件版本：v1.0 · 2026-03-11 · 设计总监审定*
*下一步：Style 2（大厂年报风）规范 · Style 6（STUDYROOM）社交内容规范*
